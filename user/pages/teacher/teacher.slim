<!DOCTYPE html>

html

  head

    == no_scaling

    title Cosmic Fit Club

    ==css ['fonts', 'site', 'header', 'footer', 'user/teacher']
    ==js  ['jquery', 'rivets', 'lib', 'element', 'ev_channel', '/auth/elements/userview', 'menu', 'user/teacher']

  body
  
    #stars
      
    .content

      .tile
        .title #{customer.name}'s Scheduled Classes:

        - customer.staff[0].schedules.select{ |x| x[:classdef_id] != 78 }.each do |sched|
          div.schedule
            span.name  #{ sched.classdef.name   }
            span.rrule #{ sched.rrule_english   }
            span.time  #{ sched.start_time_12hr }
      
      .tile.class_history
        .title Class History
        
        .history_list
          - customer.staff[0].class_history.each do |hist|
            div.class_occ
              span #{ hist[:starttime].strftime("%Y %b %e %l:%M %P") }
              span #{ hist[:classdef_name] }
              span #{ hist[:count]         }