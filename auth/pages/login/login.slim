<!DOCTYPE html>

html

  head

    == no_scaling

    title Login To Cosmic Fit Club

    == js  [ 'jquery', 'rivets', 'lib', 'element', 'ev_channel', 'elements/loginform', 'login' ]
    == css [ 'fonts', 'site', 'login']

    javascript:
      window.onload = function() {
        login = new LoginForm( id('login_container') );
      };

  body

    #login_container

    == analytics
