<!DOCTYPE html>

html

  head
    == no_scaling

    == css ['fonts', 'site', 'header', 'tuples', 'group_reservation']
    == js  ['jquery', 'lib', 'element', 'ev_channel', 'rivets' ]
    == js  ['/auth/elements/userview', '/checkout/elements/group_reservation']
    == js  ['group_reservation']
    
    javascript:
      reservation_id = #{{ params[:id] }};

  body

    #stars

    == include_slim('slim/partials/header')

    .content
      
      .tile
        group-reservation reservation='data.reservation'