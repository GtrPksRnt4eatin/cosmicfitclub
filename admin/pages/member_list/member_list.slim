<!DOCTYPE html>

html
  
  head
    
    == css ['fonts', 'site', 'header', 'admin/member_list']
    == js  ['jquery', 'rivets', 'lib', 'element', 'ev_channel', '/auth/elements/userview', 'menu', 'admin/member_list']

    title Member List
  
  body.p5

    #stars

    .tile
      
      div Subscription List
      div
        input type="checkbox" rv-checked='data.filter_options.show_deactivated'
          | Show Deactivated
      
      table
        tr 
          th Plan Name
          th Customer Name
          th Customer Email
          th Stripe ID
          th Start Date
          th End Date
        tr rv-each-sub='data.filtered_subscriptions' rv-title='sub.id' rv-data-deactivated='sub.deactivated'
          td 
            | { sub.plan_name }
          td
            | { sub.name }
          td
            | { sub.email }
          td
            | { sub.stripe_id }
          td
            | { sub.began_on }
          td
            | { sub.canceled_on }