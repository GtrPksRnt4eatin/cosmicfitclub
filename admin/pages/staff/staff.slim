html

  head

    == no_scaling

    title Cosmicfit Admin

    == css ['fonts', 'site', 'header', '/admin/staff']
    == js  ['jquery', 'rivets', 'lib', '/admin/staff']  

  body.p4

    #stars

    == include_slim('slim/partials/admin_header')

    #content

      .tile.event rv-each-staff='data.staff'
        .headshot
          img src='joy.jpg'
          .name Joy Chen
        .bio rv
        .delete rv-on-click='ctrl.del'


        img rv-src='staff.image_url'
          div.nfo
            .name 
              | { event.name }
            .eventdescription
              | { event.description }
        

      form#new
        
        img#newpreview
        input#newpic type='file' accept="image/*" name='image'
        label Event Name:
        input type='text' name='name'
        label Description:
        textarea name='description'
        span#upload Upload