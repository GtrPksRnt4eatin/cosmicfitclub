<!DOCTYPE html>

html

  head

    title
      | CFC Event Checkin

    javascript:
      data = {}
      data['events'] = #{{ Event::short_list }},
      data['filtered'] = data['events']

    == css [ 'fonts', 'site', 'event_checkin' ]
    == js_sync  [ 'jquery' ]
    == js_sync  [ 'rivets', 'moment', 'rv_dates' ]
    == js_sync  [ 'list'   ]
    == js_sync  [ 'lib', 'element', 'ev_channel', '/auth/elements/userview' ]
    == js_sync  [ 'event_checkin' ]

  body

    #stars
 
    #header
      span Cosmic Fit Club Front Desk
      #userview_container

    #content

      .tile

        .title Event Checkin
        
        #eventlist_header
          input rv-on-keyup='ctrl.filter'

        ul#eventlist
          li rv-each-event='data.filtered' rv-on-click='ctrl.open_event'
            .event
              img rv-src='event.image_url'
              .nfo
                .time 
                  | { event.starttime | dayofwk } { event.starttime | datewyr } { event.starttime | time }
                .name 
                  | { event.name }
